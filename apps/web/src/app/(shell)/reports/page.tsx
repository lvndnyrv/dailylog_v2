import { PlaceholderSection } from "@/components/shell/placeholder";

export default function Page() {
  return (
    <PlaceholderSection
      title="Reports"
      phase="Phase 5"
      blurb="Run, schedule and export reports once there's operational data to report on."
    />
  );
}
