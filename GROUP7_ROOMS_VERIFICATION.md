# Group 7 — Rooms & ratios verification

Updated: 2026-09-10. **Status: operational 7f, 7a/7c availability planning, dated demand forecasting, multi-person plans, coverage review and educator responses, the reviewed 7e vacancy-to-waitlist handoff, and scheduled release of expired enrollment offers are implemented; Group 7 is not fully complete.**

Reference: `design/designs/DailyLog WEB - Admin.dc.html`, screens 7a–7f.
Changes remain uncommitted on the existing branch. Earlier unrelated work was preserved.

## Fixed in this pass

| Area | Finding | Implemented correction |
| --- | --- | --- |
| 7a / 7b live ratios | Admin and educator used different staff-count engines; room detail added coverage again locally. | Shared database staff-presence calculation. Current temporary coverage overrides clock-in room, then home assignment. One educator counts in one effective room. Room detail uses the returned eligible educator list and names the counted staff. |
| Eligibility | Assigned profiles could count while inactive, awaiting required clearance, on approved leave, or clocked out. | Both live engines use active staff, required background clearance, approved leave, and clock-ins when tracking is enabled. |
| Coverage display | Declined/completed coverage and closed time entries could appear active. | Coverage query includes assigned/accepted records only; clock-in markers exclude closed entries. Scheduled gap checks deduplicate the same staff member appearing in a shift and coverage assignment. |
| 7d ratio settings | Per-room loop silently skipped invalid values and could partly save. | One atomic, permission-checked database operation; positive whole-number ratios and complete room availability required. |
| Alert settings | Failed optimistic saves left toggles showing unsaved values. | Restore previous settings after returned errors or exceptions; accessible save status and keyboard focus. |
| 7e planning | A full or missing suggested destination disabled the planner. | Planner opens for every candidate, supports choosing the destination, and warns when it is full today. Changing the move day updates suggested transition visits. Default dates avoid weekends. |
| 7e completion | A future move could complete immediately without checking capacity. | Reject early completion, unavailable/unopened destinations, full rooms, and over-ratio destinations when the child is checked in. Completion remains atomic and idempotent. |
| 7f settings | Morning could save before evening failed. | Atomic save of both periods, same-center active-room validation and overlapping shared-room rejection. Weekend timeline no longer shows weekday combinations. |
| 7f live operation | Combination settings previously had no effect on live operation. | Activated weekday windows resolve children and eligible educators to one host, apply the stricter configured ratio, and preserve permanent home rooms. Existing plans remain inert until explicitly saved. |
| 7f check-ins | Clients could bypass the center's blocking setting. | A database guard checks projected host capacity and combined ratio for new live check-ins when the center enables blocking. Active combination settings cannot lose their required ratios or host capacity. |
| 7f educator access | Educators could not work with the partner-room roster. | Time-bound partner-room access, shared roster/attendance/active nap loading, and temporary room selection that does not overwrite the educator's home-room profile. Access expires on pause or window end. |
| 7f controls | No way to suspend an activated schedule for one day. | Admin pause/resume controls on Rooms and Dashboard; configuration changes notify educators with a Room Ratios destination. Web and mobile refresh operating context. |

Applied migration: `20260905000100_admin_rooms_group7.sql` in linked project `uxieegqydrowgcygsnmq`.

Operational follow-up migrations also applied: `20260906000100_operational_room_combinations.sql` and `20260906000200_combination_rule_integrity.sql`.

## Verification

- Web type check, lint and production build passed. Build reports the existing multiple-lockfile workspace-root warning.
- Linked database lint: no schema errors.
- `supabase/tests/admin_rooms_group7_test.sql`: rollback-only tests for admin/educator live-count parity, coverage access, no double counting, declined coverage, clearance, approved leave, clock-in/out, educator denial of licensing changes, atomic rules/combinations, and transition completion guards/idempotency.
- Existing `admin_educator_room_coverage_handoff_test.sql` and `admin_compliance_background_clearance_test.sql` passed through the direct linked database query command.
- Browser: Rooms overview and Preschool detail rendered; Elvin's full-destination planner opened; changing move day to September 21 suggested September 14–18 visits; Cancel dismissed without saving. No real family move or notification was submitted through the browser.
- Tests use transaction rollback: temporary accounts, children, attendance, coverage, settings, notification and audit changes are not retained.
- No iOS simulator end-to-end run in this pass; cross-app parity was tested against the actual database functions used by both applications.

### Operational 7f follow-up verification

- `operational_room_combinations_test.sql` passed against the linked database: host-only aggregation, strict ratio, capacity and ratio check-in guards, clearance exclusion, temporary educator access, unauthorized pause denial, pause/resume, weekday/time boundaries, legacy-plan safety, and required room-setting protection. All fixtures rolled back.
- Re-ran `admin_rooms_group7_test.sql` and `admin_educator_room_coverage_handoff_test.sql`: passed. Linked database lint found no schema errors after both new migrations.
- Mobile and web type checks passed; web lint/build and iOS bundle export passed. No simulator was booted, so the iOS bundle is not a claim of device end-to-end verification.
- Browser Rooms and Dashboard loaded successfully after fresh navigation. An earlier dashboard error overlay did not recur; its original cause was not established.
- Browser combination dialog showed both saved periods, room/time controls, the explicit activation notice, strict-ratio explanation, and Cancel/Save actions; inspected without activating either plan.
- Combined-room gap forecasts are withheld while combined: the current home-room shift predictor cannot correctly predict across shared-room boundaries. Live ratio warnings remain available.
- Notification creation/routing was tested in the database, not delivery to a physical device. Explicit schedule changes and pause/resume notify staff; there is no separate start/end push notification. Timed operating state is refreshed by the apps and existing server sweeps.
- Shared roster, attendance and active naps are integrated. Other educator modules still need the final cross-module/device pass; do not treat this slice as completion of all educator integration.

### 7a/7c availability-planning pass — September 7

- Applied `20260906000300_room_coverage_availability.sql` and `20260907000100_coverage_source_room_safety.sql` to the linked project.
- Timeline now uses configured center opening/closing hours, excludes draft/cancelled shifts, labels assigned invitations versus accepted cover, and draws explicitly timed planned breaks. Planned breaks do not modify payroll deductions or actual clock-in status.
- Added today's published-shift break editor: save/remove a break within shift boundaries. The database rejects a break overlapping committed coverage. An untimed payroll break remains explicitly uncertain.
- Coverage picker supports a future date and checks the whole requested interval. It displays active/unavailable candidates with reasons for approved leave, required background clearance, another published room shift, planned breaks, and existing coverage. Missing covering shifts or untimed breaks require explicit admin confirmation, recorded in coverage notes.
- Planning does not assume a home-room educator is spare when their published schedule is missing. Borrowing from another home room is blocked pending the source-demand forecast. Emergency/live assignment paths retain their existing behavior while the shared integrity trigger checks known conflicts.
- Save rechecks eligibility under a staff lock. Direct coverage inserts cannot bypass known leave/break/shift conflicts. Planned saves also enforce center hours, closures, room opening date and a future end time. The existing educator invitation/access handoff remains in use.
- Room-detail gap checks split explicitly timed breaks, exclude draft shifts and unaccepted invitations, and inspect exact interval boundaries rather than skipping short gaps with a 15-minute sample. Combined-room forecasting remains withheld.
- `room_coverage_availability_test.sql` passes with transaction rollback. Regression tests `admin_rooms_group7_test.sql`, `admin_educator_room_coverage_handoff_test.sql` and `operational_room_combinations_test.sql` passed. Web typecheck, lint and production build passed; database lint passed. No simulator run was needed for this web-only pass.
- Browser: opened Rooms and the coverage picker, selected Preschool, and verified that approved leave and pending-clearance candidates were disabled while an unscheduled floater required confirmation. No real assignment, break change or notification was submitted in the browser.
- Opened the planned-break editor and visually checked the two time fields, margins and Cancel action. Closed without saving. Future credential-expiry forecasting and automatic reconciliation after later leave/schedule changes remain part of the planning follow-up; current eligibility checks are not a guarantee against subsequent changes.

### 7a/7c booking-demand forecasting — September 8

- Added **Rooms → Coverage forecast → Plan child bookings**. Admins can plan dates through the next 90 days, explicitly choosing Unknown, Expected (arrival/pickup), or Not scheduled. Saving is atomic and permission-checked. Cancel discards local edits; dismissal is disabled while a requested save is in flight.
- Bookings are planning records, not attendance, absence cancellation, tuition changes or family notifications. Existing enrollment descriptions are not silently converted into dated bookings. Unknown dates count conservatively for the whole opening day.
- Forecast demand follows dated arrival/pickup boundaries, active absences and their cancellation, scheduled departures, closures, and planned room moves. Planned moves are explicitly labelled as assumptions until completed. No permanent classroom assignments are changed by a forecast.
- Published staffing is evaluated at exact shift, break, coverage and combination boundaries. Draft shifts, approved leave, missing/expired required background clearance, overlapping contradictory room shifts and untimed breaks do not count as confirmed coverage. Accepted invitations count at their destination; pending invitations reserve staff away from the source without confirming destination staffing.
- Combined rooms count children and staff once at the host, use the stricter configured ratio, and respect active weekday windows and pause dates. The new overview forecast supersedes the earlier combined-forecast limitation above; the older room-detail live-count predictor remains a separate, limited view.
- Staff borrowing checks the entire interval after excluding the borrowed educator. Unknown source demand, untimed breaks, uncompleted source/partner-room moves, overlapping commitments and same-host transfers fail closed. Immediate loans also check actual live source attendance, not just the dated plan. Competing loan writes share a center-level lock and planned saves recheck before reserving coverage.
- Applied migrations `20260907000200` through `20260907000500`, `20260908000100_forecast_room_mapping.sql`, and `20260908000200_forecast_live_and_partner_safety.sql`. Generated database types refreshed. Materializing shared-room mappings removed repeated per-child lookups; the pre-extension forecast test dropped from roughly 18 seconds to 7 seconds wall time through the CLI.
- Rollback test `room_demand_forecast_test.sql` covers unknown and variable demand, exact one-minute break gaps, active/cancelled absences, safe/unsafe loans, second-loan depletion, pending versus accepted cover, actual attendance, combined host aggregation/strict ratios, partner-move uncertainty, pause, closures, invalid booking times and educator write denial.
- Regression tests `room_coverage_availability_test.sql`, `admin_rooms_group7_test.sql`, `admin_educator_room_coverage_handoff_test.sql` and `operational_room_combinations_test.sql` passed. The availability regression now uses unknown child demand to verify source protection, since a proven-empty source may legitimately lend staff. Web typecheck, lint and production build passed; linked database lint found no schema errors. The existing multiple-lockfile build warning remains.
- Browser: signed in with the dev owner, opened the forecast and booking editor, visually checked margins/scrolling/buttons, enabled arrival/pickup by selecting Expected, cancelled, and reopened to verify the stored plan remained Unknown. No real booking, staff loan, family message or notification was submitted in browser testing. No simulator/device test is claimed for this web/database slice.
- Boundaries: this is per-day planning, not recurring weekly booking management. Refresh after other schedule changes; there is no automatic repair/cancellation of already-planned loans when later bookings, leave or shifts change. Current checks do not guarantee future attendance. The old room-detail warning still uses current attendance rather than dated demand. These limits remain visible in the next-work list.

### 7c multi-person plans and changed-input review — September 9

- The coverage modal now builds a draft with up to 12 segments for one room/date. Different educators can cover successive portions or overlap when more than one educator is needed. Duplicate overlapping segments for the same person are disabled locally and rejected on save. Partial plans are allowed and explicitly labelled; invitation acceptance is never presented as already confirmed.
- One atomic database call validates every segment against existing commitments and all earlier segments in the batch. If any segment fails (including two individually spare educators jointly depleting a source room), assignments, plan metadata and queued notifications all roll back. A request ID makes retries safe without duplicate invitations; reusing it with different content is rejected.
- Optional room-lead notification follows the 7c design. Individual educator invitations retain the existing notification destination and access handoff. No owner/admin is automatically considered an eligible educator just because the design example suggests owner cover; explicit educator eligibility still applies.
- **Existing coverage** on the forecast card lists pending, accepted and declined assignments for the selected date. It rechecks on load, explicit refresh and every minute while the page is visible (paused while the booking/assignment editor is open). Changed source bookings, published shifts, approved leave, clearance, opening hours, closures and declines produce review reasons. Same-day refresh retains the mounted content so it does not discard an open cancellation dialog.
- Cancellation is explicit, permission-checked and retry-safe. It notifies the assigned educator, releases the commitment and refreshes the forecast. The UI explains that this can reopen a gap and points admins to Fix coverage for replacement planning. No assignments are silently cancelled or automatically reassigned.
- **Integration gap found and fixed:** the old notification copy promised acceptance, but educators had no actual response API/UI. Added an own-assignment list and Accept coverage / Decline controls to mobile Room ratios. Decline asks for confirmation; acceptance rechecks current known safety, including source-room demand, and rejects stale unsafe plans. Only the assigned active educator may respond; responses are idempotent and notify administrators in-app. Expired, cancelled or already-answered invitations cannot be answered as a new response.
- Applied `20260908000300_multi_segment_coverage_plans.sql`, `20260908000400_coverage_plan_review.sql` and `20260909000100_educator_coverage_responses.sql`; refreshed generated database types. Database lint is clean (an array-initialization warning was corrected in the follow-up migration).
- `multi_segment_coverage_plan_test.sql` passed with full rollback: atomic failure, retries/content mismatch, simultaneous source depletion, split plans, own-only educator lists, accept/decline, denial of another educator's response, rejection of unsafe changed-plan acceptance, changed bookings/leave/shifts, no silent cancellation, lead/educator notification creation, cancellation idempotency and admin-only writes.
- Re-ran demand forecasting, availability, Group 7 core, admin/educator coverage handoff and operational combinations tests: all passed. Mobile/web typechecks, web lint, production build and iOS bundle export passed. The existing multiple-lockfile build warning remains.
- Browser: opened the planner, verified required availability confirmation, added Tara and Grace to an unsaved draft, checked that duplicate overlapping selections were disabled and room/date were locked with a draft, then cancelled. No real plan or invitation was sent. The review panel rendered with no remaining assignments for the selected demo day. Rollback fixture accounts/children were removed by transaction rollback.
- Verification boundary: no simulator was booted for this slice; the iOS export is not a device interaction test. Push outbox creation and routes were verified, not delivery on hardware. Review is page-driven, not a background warning service while admins are offline. Missing published shifts/untimed breaks remain warnings even after manual availability confirmation; publishing an accurate shift/break plan is needed for automatic verification. No automatic cancellation, replacement or recurring booking calendar was introduced.

### 7d atomic room settings and lead designation — September 9

- Add room and Edit room now use one permission-checked database transaction. Name, age band, capacity, ratio, opening date, nap window and lead designation either save together or do not save. Failed lead validation no longer leaves a newly created or partially edited room behind.
- Added positive whole-number capacity/ratio validation, bounded age ranges, named custom bands, paired/ordered nap times, active-room name uniqueness, active same-center educator checks, archived-room rejection and stale-edit protection. The edit form retains its original version even if its parent refreshes. A saved form is locked until closed/reopened.
- Required fields are labelled and invalid fields receive red borders, accessible error descriptions and focus. Empty custom ages stay empty rather than silently becoming zero. Nonstandard existing age bands (for example Infant at 3–18 months) retain their label and bounds. Capacity/opening date are paired and the lead picker is full width to avoid overflow. Nap settings are also available during creation.
- Lead selection now designates responsibility only: it does **not** silently transfer the educator's primary room, grant new room access, modify shifts or change the live staffing count. This intentional safety distinction is explained in the form. Use the existing coverage workflow for staffing; a permanent/future home-room transfer workflow remains separate work, rather than an unsafe side effect of editing a room.
- Matching center-room ratios initialize the form correctly, including its first render. Suggested fallback ratios and configured values are explicitly not presented as verified licensing limits. No jurisdiction-specific legal rules were invented, and no other room's ratio is changed by this form.
- Cancel/backdrop/Escape do not dismiss an in-flight save. Cancel before submission discards local changes. The shared modal now focuses itself only when mounted, avoiding stealing focus back from the invalid input on each validation render.
- Applied `20260909000200_atomic_room_settings.sql` to the linked project and refreshed database types. `atomic_room_settings_test.sql` passed with transaction rollback, including failed-create rollback, unchanged failed updates, duplicate names, validation, foreign/inactive leads, unchanged home/access, stale versions, archived rooms, educator/view-only-admin denial and clearing lead/nap settings. Eight shared client/server validation checks passed.
- Group 7 core, operational combinations and admin–educator coverage handoff regressions passed. Database lint, web/mobile typechecks, web lint and the production web build passed. Existing multiple-lockfile build warning remains. No new mobile screen or simulator interaction was needed for this web/database slice.
- Browser checked empty required fields, reversed custom ages, focused/red invalid inputs, preserved Infant settings, and Cancel/reopen restoring the saved name. No real room, lead assignment, staffing move or notification was submitted through the browser. SQL fixtures were rolled back.

### 7e durable plans, dated capacity and reviewed completion — September 9

- The overview and room-detail candidate query now includes every active saved plan, even outside the birthday horizon, and displays the plan's chosen destination rather than a newly inferred next room. Mateo's October plan was previously hidden; it is now visible alongside birthday candidates. Archived children are not presented as actionable moves.
- Added a dated enrollment-space estimate and **Use first projected opening**. It searches from the selected day through the following 90 days, bounded by the next year, and checks room opening dates, weekdays, center closures, configured age bounds, future child start dates, scheduled departures, other valid planned moves and outstanding offer holds without duplicating enrolled children. Absences/daily bookings do not release an enrollment place. Dependent moves and offer counts are disclosed.
- Saving a plan uses an atomic, administrator/children-edit-checked call with an unchanged-version check. It rejects unavailable destinations, stale starting rooms, full dated projections, bad visit dates and visits before opening/past/closure dates. An existing plan is never silently overwritten by a competing create. Family publication retains the existing notification and parent-app route.
- Added explicit **Cancel planned move**, confirmation, stale-plan protection and retry-safe cancellation. The child stays in their current room. The linked family gets the cancellation notice and no longer sees the cancelled move in their schedule hub.
- Web completion requires the reviewed version; legacy/mobile completion callers now share the same age, dated-place, center-open-day and live operating-room capacity guards. Existing real-assignment, ratio, access and family-completion effects remain atomic and retry-safe. Future moves cannot complete early. Proposed rates are reference data, not actual billing updates.
- Important distinction: the preview is conditional, not a reservation or staffing approval. Completion retains the conservative active-enrollment capacity check, including active future-enrollment records already assigned to a room; a projected vacancy does not bypass that actual allocation check. Daily staffing coverage and transition visits still require operational planning. No licensing exception or permission to stay beyond an age band is inferred from design copy.
- Browser: confirmed Mateo's plan is visible and flagged as outside the destination's configured age band/full. Left that existing plan untouched for review. Opened Élise's planner, selected September 19, and verified that the search skipped the weekend and closure to suggest September 22; selecting it updated the draft date. Cancel discarded the draft. Layout, scrolling and disabled early-completion controls were inspected. No real move, cancellation or family notification was sent through browser verification.
- Applied `20260909000300_room_transition_planning.sql`, `20260909000400_transition_completion_recheck.sql` and `20260909000500_shared_transition_completion_guards.sql`; refreshed generated database types. New rollback test covers dated capacity, offer withdrawal, future enrollment, departures, hidden-plan visibility, stale edits/completion, early completion denial, permission isolation, real parent schedule/notification visibility, cancellation/retries, actual-capacity denial and successful completion/retry. All fixtures roll back.
- Group 7 core, demand forecast and Parent Group 27 regressions passed. The older parent test had assumed its future-dated demo move could complete immediately; its rollback fixture now explicitly sets a due open date, an age-compatible child and destination capacity before testing successful completion. Production data was not changed to make that test pass.
- Web/mobile typechecks, web lint/build and database lint passed. Existing multiple-lockfile build warning remains. Parent schedule API/notification records were exercised under the parent role, not an iOS simulator or physical push-delivery test.

### 7e persistent waiting queue and effective tuition handoff — September 10

- Added a real staff-only **Waiting for a spot** record. It retains the child, current room, requested destination, earliest acceptable date and internal note across reloads. It is visible on both the Rooms overview and source-room detail, including children outside the birthday horizon. Parents cannot read a waiting request and are not notified merely because one is saved.
- The queue recomputes the first safe projected opening through the next year from current room openings, weekdays/closures, destination age band, enrollment capacity, future starts, departures, offer holds and other planned moves. A changed child room, archived room or incomplete age data stays visible as **Needs review** instead of silently disappearing. This remains a projection, not a reservation.
- Admins may explicitly review and schedule the projected date. Conversion rechecks capacity through the existing transition planner and atomically links the waiting request to the new plan. Stale edits, competing plans, parent access, early dates and mismatched child/room details are rejected. Cancellation is explicit and retry-safe.
- Added effective-dated child tuition records for room moves. Saving a plan with a proposed rate creates or updates a scheduled rate. Cancellation cancels it; completing the guarded room move activates it and closes the prior effective rate the day before. Existing invoice totals and lines are never rewritten.
- Billing → Plans now shows scheduled/effective child rate changes. New Invoice can use the child’s current effective room rate as an explicit line-item prefill; it is never applied without the admin choosing it. Family-visible move details continue to disclose the proposed rate through the established parent schedule flow.
- Applied `20260910000100_room_transition_waiting_queue.sql` and `20260910000200_transition_tuition_effective_rates.sql` to the linked project and refreshed generated types. New rollback tests cover persistence, privacy, changing capacity, stale edits, conversion, exact family handoff, cancellation, scheduled/effective rates, prior-rate closure, linked-parent visibility and immutable historical invoices.
- Group 7 core, durable transition and Parent Group 27 regressions passed. Database lint, web/mobile typechecks, web lint and production build passed. The existing multiple-lockfile build warning remains.
- Browser: verified the new waiting action in Elvin’s full-destination planner without submitting it, and verified Billing → Plans renders Mateo’s scheduled October rate. No live waiting request, family notification, move, cancellation or invoice was created during the browser check.

### 7e reviewed vacancy release to Group 2 enrollment — September 10

- Completing a room move now creates exactly one durable review for the newly free source-room spot. Planned, cancelled and retried moves cannot create duplicate vacancies. The earliest pending vacancy for a room is the only actionable one, preventing two administrators from offering the same place through overlapping reviews.
- Enrollment → Overview displays **Newly available spots** with the moved child, source room, opening date, live waitlist count, the next eligible family, and any blocking reason. Admins must open the review and explicitly confirm the offer; unmatched spots remain visible for follow-up rather than disappearing.
- Candidate selection is deterministic and revalidated at send time. It respects requested room/start date, center open days and closures, the child’s date of birth against the room age band, sibling/staff priority, waitlist order, active/future enrollments, scheduled departures, planned moves and existing offer holds.
- The shared offer operation is now atomic and retry-safe for both vacancy reviews and ordinary **Make offer** actions. It locks center capacity, rejects a stale review or an attempt to skip the current recommended family, updates the enrollment and review together, and queues one secure offer notification. A second offer cannot silently double-book the dated place.
- Applied `20260910000300_room_vacancy_waitlist_offer_review.sql` and refreshed generated database types. `room_vacancy_waitlist_offer_review_test.sql` passed with transaction rollback, including creation/retry, priority, age/email eligibility, dated capacity changes, parent denial, stale review rejection, recommendation enforcement, one notification, idempotent retry, double-booking denial, unmatched vacancies and cancelled moves.
- Regression discovery: `20260904000100_admin_dashboard_group9.sql` had replaced the shared notification guard with a shortened version, dropping the narrow parent enrollment receipt checks. Applied `20260910000400_restore_notification_enqueue_guards.sql`, which restores those checks while retaining the dashboard notification mappings. Both Parent Group 24 enrollment suites pass again.
- Group 7 core, transition planning, waiting queue and tuition regressions passed. Linked database lint, web/mobile typechecks, web lint and production build passed. The existing multiple-lockfile build warning remains. Browser submission was intentionally not used because no production move was completed merely to manufacture a vacancy; UI construction and the actual role-scoped database lifecycle are covered by the rollback test.

### Group 2j closed-offer advancement — September 10

- Declined, expired and administrator-withdrawn offers now release their room hold into the same durable capacity-review queue. The configured automation identifies a successor; it no longer silently sends a new offer from an old browser preview. Enrollment explains that an administrator must confirm the current priority, age fit, first day, capacity and terms.
- The former multi-step web action is one permission-checked transaction: offer closure, optional return to the bottom of the public waitlist tier, center-wide reindexing, family notice and review creation either all commit or all roll back. Retries do not duplicate the review or withdrawal message.
- A released family is excluded from its own successor review even when kept on the waitlist. Invalid age or contact data are skipped, live occupancy is rechecked, and the successor remains untouched until **Confirm & send offer** uses the shared atomic offer function. Turning the automation setting off leaves the room open for the existing manual **Make offer** flow.
- Applied `20260910000500_reviewed_offer_release_advancement.sql` and refreshed generated database types. `offer_release_review_test.sql` passed with rollback, covering atomic withdrawal, no silent successor mutation, source-family exclusion, invalid-age skipping, one notice/review, idempotency, confirmed successor handoff and a parent-declined offer entering the same queue.
- Re-ran the transition-vacancy, Parent Group 24 base/completion, transition-planning and waiting-queue suites: all passed. Linked database lint, both app typechecks, web lint and production build passed. Browser verified the real Baker withdrawal modal shows the safe queue preview and confirmation boundary; Cancel closed it without changing production data.

### Group 2m scheduled departure release — September 10

- Scheduling or updating a child’s last day is now one administrator-only database operation with same-center active-child, required-field, length and non-past-date validation. A concurrent duplicate cannot leave two active departure records.
- A linked hourly database worker evaluates each center in its own time zone. On the first run after the last day it marks the departure complete, archives rather than deletes the child, closes the linked enrollment pipeline record and preserves the full profile in Alumni. Repeated worker runs are idempotent.
- **Prepare this spot for waitlist review** now means what the application does: the released room is matched through the shared priority, age, open-day and dated-capacity checks, then waits for administrator confirmation. Turning it off holds the room for a transfer or sibling and creates no review. No family is silently offered a spot.
- Applied `20260910000600_scheduled_departure_spot_review.sql` and refreshed generated types. `scheduled_departure_review_test.sql` passed with rollback, covering scheduling validation, due processing, Alumni preservation, optional review creation, safe matching, retry deduplication, held spots, parent denial and the installed scheduler.
- Offer-release, transition-vacancy, transition-waiting and Parent Group 24 regressions passed. Linked database lint, both app typechecks, web lint and production build passed. Browser opened David’s real withdrawal planner, verified the updated review explanation and controls, then cancelled without saving.

### Group 2j background offer expiry — September 10

- Offers that pass their expiration time are now closed by a linked database worker every five minutes even when the family never reopens the secure offer link. The same operation still runs when an authorized administrator invokes it manually; parents cannot invoke the sweep.
- Expiry removes the stale room hold and returns the family to the active inquiry waitlist instead of leaving an expired offer displayed as open. When reviewed advancement is enabled, the released room creates one durable successor review; when disabled, the spot stays outside that workflow for manual handling.
- The source family is excluded from that particular successor review and the next family remains completely unchanged until an administrator performs the existing live capacity/age/priority check and confirms the offer. Repeated sweeps do not duplicate vacancy reviews.
- Applied `20260910000700_expired_offer_sweep.sql` and refreshed generated database types. `expired_offer_sweep_test.sql` passed with rollback, including stage normalization, successor non-mutation, review creation, retry deduplication, disabled automation, parent denial and scheduler installation.
- Re-ran offer-release, transition-vacancy, scheduled-departure, transition-waiting and both Parent Group 24 suites: all passed. Linked database lint reported no schema errors.

### Group 2m atomic Alumni re-enrollment — September 10

- Alumni re-enrollment now restores the child, chosen room and exactly one linked enrollment record in a single administrator-only transaction. A failure cannot leave an active child with a withdrawn pipeline record, or the reverse.
- The operation rechecks same-center active room, opening date, current age band and projected capacity under the shared center lock. Completed departure history remains intact; obsolete scheduled departures, planned room moves and waiting requests are cancelled before the profile becomes active again. A same-request retry returns the existing result rather than duplicating the enrollment.
- Alumni without an older pipeline row receive one enrolled history row. Existing application/history data and parent-child links are not deleted. Parents and administrators missing either enrollment-edit or children-edit access cannot call the operation.
- The Alumni modal now gives each child a non-cramped full-width room selector, marks the room as required, labels full/ineligible/not-yet-open rooms, and renders an accessible red error state when the server rejects a selection.
- Applied `20260910000800_atomic_alumni_reenrollment.sql` and refreshed generated types. `atomic_alumni_reenrollment_test.sql` passed with rollback, including zero partial writes on a full room, room/age/capacity guards, history preservation, stale-work cleanup, pipeline recovery, retry and parent denial.

### Group 2f hardened admin offer completion — September 10

- The legacy web completion path now accepts only a family-accepted offer with completed application/agreement and any required deposit recorded. It cannot change the room after acceptance, use another center’s room, bypass age/open-day/dated-capacity checks or create a child from a stale offer.
- The accepted offer already counts as one projected room hold, so finalization converts that hold into the roster child without double counting. Allergy and emergency-contact application data are carried into the child profile, onboarding state is finalized atomically, and retries return the same child.
- Pipeline cards no longer claim every open offer is accepted. Families still completing their secure flow show **Family onboarding in progress** and open the lifecycle; the completion control appears only for a genuinely ready legacy/manual record. The completion modal fixes the room to the one the family accepted and displays acceptance, deposit and paperwork readiness.
- Applied `20260910000900_harden_admin_pipeline_enrollment.sql`. `hardened_admin_pipeline_enrollment_test.sql` passed with rollback, including unaccepted-offer denial, accepted-room immutability, capacity invalidation, application-data carryover, atomic finalization, retry and parent denial.
- Re-ran the Alumni, offer-expiry, offer-release, transition-vacancy, scheduled-departure and both Parent Group 24 suites. Linked database lint, both app typechecks and web lint passed. Browser visual verification is pending only because the Mac was locked; no production enrollment was submitted.

### Group 2j atomic waitlist check-ins — September 10

- Administrator **Still interested?** reminders now commit the selected family updates and outbound messages as one transaction. An invalid, inactive, unreachable or already-pending family rejects the entire batch instead of leaving some families updated and others unsent.
- Each valid message opens one seven-day response window. The admin list hides families while that window is open, and the database rejects duplicate reminders even from a stale browser. Families without a guardian email/secure journey link are visibly unavailable rather than failing after selection.
- The existing overdue processor is now scheduled hourly. Once a response window ends it applies the center’s configured unanswered threshold, removes stale entries from the ranked list and reindexes the queue without deleting the family record. The existing secure parent response resets the unanswered count when the family confirms.
- Applied `20260910001000_atomic_waitlist_checkins.sql` and refreshed generated types. `atomic_waitlist_checkins_test.sql` passed with rollback, covering mixed-batch rollback, two-family delivery, secure routing payloads, duplicate prevention, scheduled archival and parent denial.

### Group 2k atomic waitlist placement and rules — September 11

- **Add to waitlist** now validates the active center/room, desired start, child age at that start and guardian contact, then saves placement, rule-aware center-wide ranking and the secure family confirmation in one transaction. A retry preserves the original waiting age and never duplicates its notice.
- **Waitlist rules** now validates every bound, saves all settings together, re-ranks under the same center lock and queues position-change messages only for families that actually moved. The shared reindexer now respects disabled sibling/staff priority rather than always forcing those tiers first.
- Expired offer rows are excluded from the live count and open-offer card even if a tab remains open across the expiry boundary. Browser refresh confirmed the background worker had converted Mia's expired hold into a visible administrator-reviewed Infant vacancy, with no offer still shown as open.
- Applied `20260911000100_atomic_waitlist_management.sql` and refreshed generated types. `atomic_waitlist_management_test.sql` passed with rollback, covering parent/foreign-room denial, age mismatch rollback, desired-date persistence, sibling priority, retry safety, one secure confirmation, chronological re-ranking, family position notices and invalid-settings rollback.

### Group 2l atomic inquiry closure — September 11

- Closing an inquiry now commits pipeline closure, waitlist removal/reindexing, open-offer withdrawal, reviewed vacancy creation, future-tour release and the optional goodbye message together. It preserves the historical record and treats a repeated click as a safe retry.
- Enrolled children must use scheduled withdrawal, and accepted offers must be completed or explicitly withdrawn first. A goodbye cannot be promised without a deliverable guardian email.
- Applied `20260911000200_atomic_inquiry_closure.sql`. `atomic_inquiry_closure_test.sql` passed with rollback, including parent denial, enrolled-family protection, offer/vacancy handoff, reusable future tour slots, contiguous ranking, one secure family notice and retry safety.

### Group 2m atomic administrator tour lifecycle — September 11

- Administrator booking now uses the same locked, secure tour transaction as the parent journey. It validates the family, center, active host and live slot; atomically releases an older reservation; synchronizes the pipeline card; and queues both confirmation and next-day reminder. Competing bookings and retries cannot double-book or duplicate notices.
- Cancellation now uses an allowed persisted `cancelled` outcome, reopens the future slot, removes stale pending reminders, returns a tour-only family to inquiry and queues a replacement cancellation notice. Previously, the web action attempted to store a value rejected by the database constraint.
- Tour outcomes are guarded and atomic. An attended tour can move the family to Application and queue the secure application handoff in the same commit; no-show and reschedule remain explicit outcomes, and rescheduling releases a future reservation/reminders for rebooking.
- Applied `20260911000300_atomic_admin_tour_lifecycle.sql`. `atomic_admin_tour_lifecycle_test.sql` passed with rollback, covering permissions, invalid hosts, replacement-slot release, competing booking rejection, synchronized state, two-message delivery, booking/cancellation/outcome retries and attended-to-application handoff.

### Group 2n–2p enrollment documents, parent application handoff and fit checks — September 12

- Administrator document requests now save every requested status and queue the family message in one transaction. Structured requested/received timestamps are shared by the web application review and parent document flow; retries do not duplicate requests. Pre-offer handbook signing remains unavailable until an actual offer exists.
- An attended-tour application handoff now opens a real parent application instead of a journey screen with no completion path. The family can save contacts/health details, upload requested private documents and finish the pre-offer application, then returns to the journey tracker. Agreement and payment remain offer-gated.
- The application **Fit check** no longer reports three unconditional green claims. Age is calculated on the chosen first day against the configured room band. Capacity uses the same dated occupancy projection enforced at offer submission, including future enrollments, moves, departures and active offer holds without counting the current offer twice.
- Staffing is clearly advisory and uses the published first-day demand forecast after adding this child and other active offer holds. It distinguishes confirmed coverage, pending/unresolved coverage, a hard educator gap and missing planning data. It does not pretend that today’s room roster proves a future ratio.
- The offer room picker now labels current enrollment as current context, not future availability. Final submission still atomically rechecks room status, center closures, first day, age and dated capacity; staffing gaps remain visible for planning rather than silently blocking a future offer.
- Applied `20260911000400_atomic_enrollment_document_requests.sql`, `20260911000500_parent_preoffer_application_handoff.sql` and `20260912000100_enrollment_fit_check.sql`; refreshed generated types. Their rollback suites passed, including request atomicity, secure parent handoff, private upload, offer gating, dated fit, current-offer idempotency, age/capacity changes, ratio gaps and parent denial. Linked database lint and web/db typecheck/lint passed.

### Group 2f first-day onboarding handoff — September 12

- A paid future enrollment remains available to administrators and the linked family for preparation, but no longer leaks onto an educator's child list, room roster, daily-log surfaces or attendance access before the agreed first day. Administrator attendance writes are also rejected before that date, so a privileged or stale client cannot bypass the lifecycle boundary.
- The door kiosk now follows the same rule. A future child is not returned from PIN lookup and cannot be checked in early, even though an approved pickup credential may already exist for setup.
- **Open lifecycle** now reflects five real milestones instead of treating acceptance as deposit payment or a stored first-day flag as attendance activation. The first day completes from the center-local date and the child's agreed start date.
- The missing **Room handoff** is implemented: administrators choose an active educator assigned to the accepted room, enter a unique cubby label, and save both values to the child and enrollment atomically. A welcome email is queued for 9:00 AM three days before the first day, or immediately when that point has passed.
- Applied `20260912000200_first_day_onboarding_handoff.sql` and `20260912000300_kiosk_first_day_boundary.sql`; refreshed generated types. `first_day_onboarding_handoff_test.sql` passed with rollback, covering admin/parent preparation access, educator hiding, current-child access, direct attendance and kiosk rejection, primary-educator validation, stored room/cubby handoff, scheduled welcome delivery and no premature exposure after setup.
- The accepted offer's monthly tuition is now stored as a durable child tuition rate. Future starts remain **scheduled** and an hourly center-time-zone worker makes the rate **effective** on day one; same-day starts activate immediately. Parents can see their linked child's rate, educators cannot, and closing the enrollment ends its open rate without touching historical invoices.
- Applied `20260912000400_enrollment_tuition_activation.sql`. `enrollment_tuition_activation_test.sql` passed with rollback, covering immediate and future activation, lifecycle state, parent visibility, educator privacy, worker permission and withdrawal closure. The lifecycle now shows the real monthly amount and scheduled/effective/no-charge status rather than merely promising that billing will start.
- Enrollment completion now creates a durable monthly billing schedule and the first tuition invoice on the agreed start date. A paid deposit is applied once through the normal family ledger; a prepaid first month settles that invoice without charging it again, and excess deposit credit carries forward. Retry-safe invoice/payment keys prevent duplicate charges, and only a remaining balance produces a family notice.
- Completed room-transition tuition changes now feed the recurring invoice for the service period in which the effective rate applies. A planned transition cannot change billing early, catch-up periods use their historically effective rate, and issued invoices remain unchanged. The billing schedule and enrollment lifecycle advance to the applied rate after generation; mid-period proration remains a separate policy decision.
- Applied `20260912000500_enrollment_first_invoice_handoff.sql` and `20260912000600_effective_tuition_invoice_handoff.sql`. Their rollback suites passed alongside the tuition-activation, first-day, hardened pipeline, fit-check, document-request and parent Group 24 enrollment regressions. The production web build, app/database typechecks, web lint and linked database lint passed. One parent-consent fixture was made independent of the shared seed child's archived state; it now passes without changing live data.
- The educator roll-call API had one elevated-read gap: it correctly rejected early check-in but still listed a future child as awaited. Applied `20260912000700_mobile_roll_call_first_day_boundary.sql`, which removes pre-start children and recomputes all roll-call totals from the visible rows. The integrated day-one cycle now proves that the child is absent before the start date, then reaches educator roster, roll call, attendance, pickup, daily log and messaging without another administrator action.
- `enrollment_educator_day_one_cycle_test.sql`, the updated first-day suite, Mobile Group 23 attendance, admin attendance, pickup safety and staff messaging all passed with rollback. The long-lived attendance fixtures now rebuild their date-relative scenario inside the transaction, so weekends, archived demo rows and an old seed date cannot create false failures or mutate live data.

### Educator daily-log publication handoff — September 12

- **Send to parents** no longer marks a daily log sent and then separately attempts a best-effort notification. `publish_daily_log` now permission-checks, finalizes the report and queues the linked family delivery in one transaction. A delivery failure rolls back the sent state; a retry preserves the original sent time and cannot duplicate the family notice.
- Future reports and reports before the child's enrollment date cannot be published. Parents cannot invoke the staff publication operation. The notification includes the daily-log, child and date identifiers needed to open the exact recap.
- Applied `20260912000800_atomic_daily_log_publication.sql` and moved the educator mobile client to that operation. `atomic_daily_log_publication_test.sql`, the isolated rolling-week Parent Groups 5–6 suite and the enrollment-to-educator day-one suite passed with rollback. Mobile typecheck, the Expo 56 iOS production bundle export and linked database lint passed.

### Educator incident and medication safety handoff — September 12

- Linked families now retain read-only incident history after a child moves to Alumni. Educators lose child, incident and medication operational access immediately when the child is archived; administrators retain the access required to manage Alumni records.
- The complete incident lifecycle is covered in one rollback flow: educator draft/submission, director notification and sign-off, parent notification and acknowledgment, staff feedback, and family history after archive. Routine reports remain private until sign-off; serious reports continue to reach the family immediately without bypassing director review.
- Admin incident sign-off no longer reports a false success when another administrator has already reviewed the record. A zero-row conditional update returns a clear stale/unavailable result instead.
- Applied `20260912000900_alumni_safety_record_continuity.sql`. Both Parent Group 20 suites and the new `educator_admin_incident_handoff_test.sql` pass against the linked project.
- All four Parent Group 23 medication suites now build current, rollback-only authorizations and active-child context. Their dates use the center timezone rather than the database session date, so they remain valid across seed age and UTC midnight. They cover signed evidence immutability, renewal rules, five-right checks, witness enforcement, dose/route integrity, duplicate and daily-dose guards, atomic high-priority parent notification, automatic expiration, and the Alumni access boundary.
- The complete 76-file linked rollback matrix returned clean. Because the run occurred on a closed center day, successful same-day transition branches were explicitly deferred in the five suites that require a live open weekday; those paths have prior recorded open-day passes, while this run verified their closed-day guards. Linked database lint and both app typechecks passed.

## Still required before calling Group 7 complete

1. **7a / 7c remaining integration:** connect the room-detail warning to the new dated forecast. Add recurring booking management and future-day break editing; the break editor currently covers today's published shifts. Invitations without accounts still need a separate pending-invite pool card. Background notifications for invalidated plans and a device-level coverage invitation/response pass remain outstanding.
2. **7d remaining boundary:** a confirmed permanent/future home-room transfer for a newly designated lead is not implemented by the room form. Leadership and actual staffing are now intentionally separate; no licensing-rule authority/preset verification is claimed.
3. **7e remaining handoffs:** the waiting queue recomputes when an admin opens the page but does not silently create/publish moves in the background. Completed moves, closed or automatically expired offers, and configured child departures feed an explicit reviewed waitlist offer; offers are never sent without admin confirmation. Effective transition rates now change future recurring invoices, but mid-period proration is not claimed. Background invalidation warnings remain beyond the conditional capacity preview.
4. **Final UI/device pass:** all design variants with realistic busy-day fixtures, narrow layouts, permission-restricted administrators, and educator simulator navigation, including active shared-room workflows and return to home-room access.

## Faster database verification

The installed CLI supports `supabase db query --linked --file <test.sql>` through the Management API. Rollback SQL tests no longer require dashboard UI or a local Docker database. Never remove the transaction rollback from these verification fixtures when running against the shared project.
